server:
  port: 0
spring:
  profiles:
    active: @profileActive@
  application:
    name: trade-service

morefans:
  mybatis:
    type-aliases-package: com.morefans.trade.pojo
    mapper-location: classpath*:/mybatis/*.xml
    config-location: classpath:mybatis-config.xml
    mapper-package: com.morefans.trade.mapper
    check-config-location: true


management:
  endpoints:
    web:
      exposure:
        include: '*'